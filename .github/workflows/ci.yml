name: CI

on:
  push:
    branches:
      - main
  pull_request:

concurrency:
  group: ci-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Validate website
        run: pnpm --filter @henryqw/pi-docs run validate
      - name: Run tests
        shell: bash
        run: |
          set +e
          pnpm test 2>&1 | tee "$RUNNER_TEMP/pnpm-test.log"
          status=${PIPESTATUS[0]}
          if (( status != 0 )); then
            {
              echo "### Failed tests"
              grep -E '^(not ok [0-9]+ - |# fail [1-9])' "$RUNNER_TEMP/pnpm-test.log" || echo "pnpm test failed before producing a test summary; see the full Run tests log."
            } | tee -a "$GITHUB_STEP_SUMMARY"
          fi
          exit "$status"
      - run: pnpm run typecheck
      - run: pnpm run pack:check
      - name: Require package version bumps
        if: github.event_name == 'pull_request'
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: node scripts/check-package-versions.mjs "$BASE_SHA" "$HEAD_SHA"
