# Pi Herdr Done

Pi Herdr Done ends completed worktree work through Herdr, with destructive cleanup requiring explicit force.

## Language

**Worktree completion**:
Explicit end of a Pi task that closes its Herdr workspace and removes its linked Git worktree checkout. Dirty checkout removal requires explicit force.
_Avoid_: tab close, Pi exit, implicit forced cleanup
