---
import PageLayout from "blume/components/layout/PageLayout.astro";
import data from "blume:data";

import Footer from "../components/Footer.astro";
import { extensions } from "../extension-catalog.ts";
import { npmBadgeUrl } from "../npm-badge.ts";

const { config } = data;
---

<PageLayout
  site={{ title: config.title, description: config.description }}
  logo={config.logo}
  banner={config.banner}
  analytics={config.analytics}
  navigation={data.navigation}
  favicon={config.favicon}
  appleIcon={config.appleIcon}
  fontCssVars={data.fontCssVars}
  themeMode={config.theme.mode}
  searchEnabled={config.search.enabled}
  siteUrl={config.site}
  ogEnabled={config.og.enabled}
  ogImage="/og.png"
  ogImageAlt="Henry Pi Harness — Built on Pi. Tuned by Henry."
  ogImageSize={{ width: 1200, height: 630 }}
  page={{ title: "Henry Pi Harness", description: config.description }}
>
  <div class="mx-auto max-w-360 px-6 py-10 md:px-10 md:py-16 lg:px-16 lg:py-20">
    <header class="grid max-w-4xl items-center gap-6 sm:grid-cols-[minmax(0,1fr)_auto]">
      <div class="max-w-2xl">
        <h1 class="text-5xl font-medium leading-none tracking-tight md:text-6xl">Henry Pi Harness</h1>
        <div class="mt-5 flex items-center gap-3">
          <img
            class="size-14 rounded-full md:size-16"
            src="/apple-icon.png"
            alt="Henry Wang"
            width="56"
            height="56"
          />
          <p class="text-lg text-muted-foreground md:text-xl">Built on Pi. Tuned by Henry.</p>
        </div>
      </div>
      <img
        class="size-16 rounded-blume outline outline-black/10 sm:size-24 dark:outline-white/10"
        src="/pi-logo.svg"
        alt="Pi logo"
        width="96"
        height="96"
      />
    </header>

    <figure class="mt-10 overflow-hidden rounded-blume border border-border bg-muted/30 shadow-sm md:mt-14">
      <img src="/example.png" alt="Pi running with several extensions from this repository" />
    </figure>

    <section id="extensions" class="mt-16 scroll-mt-20 md:mt-20">
      <div class="flex items-baseline justify-between gap-4 border-border border-b pb-4">
        <h2 class="text-2xl font-medium tracking-tight">Extensions</h2>
        <span class="text-sm text-muted-foreground">{extensions.length} public extensions</span>
      </div>
      <div class="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">
        {
          extensions.map((item) => (
            <a
              class="group flex h-full flex-col rounded-blume border border-border bg-background p-5 transition-colors hover:bg-muted/60"
              href={`/extensions/${item.directory}`}
            >
              <div class="flex items-start justify-between gap-3">
                <h3 class="break-words font-mono text-sm font-medium group-hover:text-accent">
                  {item.name}
                </h3>
                <span class="shrink-0 font-mono text-xs text-muted-foreground">v{item.version}</span>
              </div>
              <p class="mt-2 flex-1 text-sm text-muted-foreground">{item.description}</p>
              <div class="mt-4 border-border border-t pt-3">
                <img
                  alt="Monthly npm downloads"
                  class="h-5 w-auto"
                  decoding="async"
                  height="20"
                  loading="lazy"
                  src={npmBadgeUrl(item.name)}
                  width="144"
                />
              </div>
            </a>
          ))
        }
      </div>
    </section>
  </div>
  <Footer slot="footer" />
</PageLayout>
